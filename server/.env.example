# Documentation only: the app reads process environment, not .env files.
# Replace with EdgeSight's ID from chrome://extensions (exactly 32 a-p letters).
EDGESIGHT_EXTENSION_ORIGIN=chrome-extension://aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
